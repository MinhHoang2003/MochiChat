<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vnjp.monstarlablifetime.mochichat">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".screen.authenication.login.RegisterActivity"></activity>
        <activity android:name=".screen.home.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".screen.authenication.login.LoginActivity" />
        <activity android:name=".screen.introduction.IntroductionActivity" />
        <activity android:name=".screen.chat.ChatActivity"></activity>
        <activity android:name=".screen.splash.SplashActivity">
        </activity>
    </application>

</manifest>